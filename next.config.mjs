/** @type {import('next').NextConfig} */
const nextConfig = {
    
};

// const nextConfig = {
//   webpack: (config) => {
//     config.externals = [...config.externals, 'argon2'];
//     return config;
//    },
// };

export default nextConfig;
